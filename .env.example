# Copy to .env.local
# Required for ARIA chat (POST /api/chat uses OpenAI directly, not Vercel AI Gateway).
OPENAI_API_KEY=

# Optional: require Bearer token for POST /api/hub-data and POST /api/hub-data/upload
HUB_ADMIN_TOKEN=
